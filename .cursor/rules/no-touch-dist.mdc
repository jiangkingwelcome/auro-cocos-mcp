---
description: 打包产物目录禁止直接修改
globs: dist/**,dist-plugin/**,dist-release/**,dist-plugin-release/**
alwaysApply: false
---

# 禁止修改打包产物

这些目录中的所有文件都是 TypeScript 编译产物，由 `npm run build` 自动生成。

**绝对禁止**：
- 直接编辑、创建或删除 `dist/`、`dist-plugin/`、`dist-release/`、`dist-plugin-release/` 中的任何文件
- 用 sed/awk/echo 等方式修改这些目录中的 .js/.d.ts/.js.map 文件

**正确做法**：
- 编辑 `src/` 下的 TypeScript 源文件
- 运行 `npm run build` 重新编译
